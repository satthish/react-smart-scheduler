/**
 * HeadlessScheduler — the core Scheduler with no extra styling opinions.
 * Import this when you want to bring your own CSS from scratch.
 *
 * Usage:
 *   import { HeadlessScheduler } from 'react-smart-scheduler/adapters';
 *   import 'react-smart-scheduler/dist/scheduler.css'; // base structure styles only
 */
export { Scheduler as HeadlessScheduler } from '../../Scheduler';
